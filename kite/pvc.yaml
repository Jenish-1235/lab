apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kite-data
  namespace: kite
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard